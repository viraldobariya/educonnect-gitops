apiVersion: external-secrets.io/v1
kind: ClusterSecretStore

metadata:
  name: ps-cluster-secret-store

spec:
  provider:
    aws:
      service: ParameterStore
      region: us-east-1
      # role: <role_arn>  // in case if you want that pod should adopt this role while fetching secrets